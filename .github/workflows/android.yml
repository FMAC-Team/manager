name: Android CI

on:
  workflow_dispatch:
  push:
    branches: [ "main" ]
    paths:
      - "app/src/**/*.kt"
      - "flutter/lib/**/*.dart"
      - "build.gradle.kts"
      - "gradle/libs.versions.toml"
  pull_request:
    branches: [ "main" ]
    paths:
      - "app/src/**/*.kt"
      - "flutter/lib/**/*.dart"
      - "build.gradle.kts"
      - "gradle/libs.versions.toml"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: gradle

    - name: Set up Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.41.6'
        cache: true

    - name: Cache Flutter pub
      uses: actions/cache@v4
      with:
        path: ~/.pub-cache
        key: pub-${{ hashFiles('flutter/pubspec.lock') }}
        restore-keys: pub-

    - name: Cache Flutter AAR
      id: cache-aar
      uses: actions/cache@v4
      with:
        path: flutter/build/host/outputs/repo
        key: flutter-aar-${{ hashFiles('flutter/pubspec.lock', 'flutter/lib/**/*.dart') }}

    - name: Build Flutter AAR
      if: steps.cache-aar.outputs.cache-hit != 'true'
      working-directory: flutter
      run: |
        flutter pub get
        flutter build aar --no-debug --no-profile
        
    - name: Upload Flutter AAR Artifact
      uses: actions/upload-artifact@v6
      with:
        name: flutter-aar-repo
        path: flutter/build/host/outputs/repo/
        if-no-files-found: error

    - name: Cache Zig 0.15.2
      id: cache-zig-0.15.2
      uses: actions/cache@v4
      with:
        path: zig-x86_64-linux-0.15.2
        key: zig-0.15.2
        
    - name: Cache Zig 0.16.0
      id: cache-zig-0.16.0
      uses: actions/cache@v4
      with:
        path: zig-x86_64-linux-0.16.0
        key: zig-0.16.0

    - name: Download Zig
      if: steps.cache-zig.outputs.cache-hit != 'true'
      run: |
        curl -OL https://ziglang.org/download/0.15.2/zig-x86_64-linux-0.15.2.tar.xz
        curl -OL https://ziglang.org/download/0.16.0/zig-x86_64-linux-0.16.0.tar.xz
        tar -xf zig-x86_64-linux-0.15.2.tar.xz
        tar -xf zig-x86_64-linux-0.16.0.tar.xz

    - name: Build ncore
      run: |
          git clone https://github.com/FMAC-Team/ncore.git
          mkdir -p app/src/main/jniLibs/arm64-v8a/
          cd ncore
          ../zig-x86_64-linux-0.15.2/zig build \
            -Dtarget=aarch64-linux-android \
            -Doptimize=ReleaseSafe
           cp zig-out/lib/libncore.so ../app/src/main/jniLibs/arm64-v8a/
          git checkout zig0.16
          ../zig-x86_64-linux-0.16.0/zig build -Dtarget=aarch64-linux-android -Doptimize=ReleaseSafe -Dndk-sysroot=$ANDROID_HOME/ndk/29.0.14206865/toolchains/llvm/prebuilt/linux-x86_64/sysroot
          cp zig-out/lib/libncore2.so ../app/src/main/jniLibs/arm64-v8a/

    - name: Grant execute permission for gradlew
      run: chmod +x gradlew

    - name: Build with Gradle
      run: ./gradlew assembleRelease

    - name: Upload Release Artifact
      uses: actions/upload-artifact@v6
      with:
        name: release
        path: app/build/outputs/apk/release/*.apk
