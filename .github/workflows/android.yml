name: Android CI

on:
  workflow_dispatch:
  push:
    branches: [ "main" ]
    paths:
      - "app/src/**/*.kt"
      - "build.gradle.kts"
      - "gradle/libs.versions.toml"
  pull_request:
    branches: [ "main" ]
    paths:
      - "app/src/**/*.kt"
      - "build.gradle.kts"
      - "gradle/libs.versions.toml"
      
jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
      with:
       fetch-depth: 0
    - name: set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: gradle
        
    - name: Set up Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: 3.41.6
        cache: true
        
    - name: Build Flutter AAR
      working-directory: flutter_nekosu
      run: |
        flutter pub get
        flutter build aar --no-debug --no-profile
       
    - name: Copy Flutter AARs to app/libs
      run: |
       mkdir -p app/libs
       cp flutter_nekosu/build/host/outputs/repo/me/nekosu/flutter_nekosu/flutter_release/*/flutter_release-*.aar app/libs/
    # flutter engine embedding
       find ~/.pub-cache -name "flutter_embedding_release-*.aar" | head -1 | xargs -I{} cp {} app/libs/
    # 其他 flutter plugin AAR（如有）
       find flutter_nekosu/build/host/outputs/repo -name "*.aar" | xargs -I{} cp {} app/libs/
       echo "=== libs contents ==="
       ls -la app/libs/

    - name: Grant execute permission for gradlew
      run: chmod +x gradlew
    - name: build ncore
      run: |
        git clone https://github.com/FMAC-Team/ncore.git
        curl -OL https://ziglang.org/download/0.15.2/zig-x86_64-linux-0.15.2.tar.xz
        tar -xf zig-x86_64-linux-0.15.2.tar.xz
        cd ncore
        ../zig-x86_64-linux-0.15.2/zig build -Dtarget=aarch64-linux-android -Doptimize=ReleaseSafe
        cd ..
        mkdir -p app/src/main/jniLibs/arm64-v8a/
        cp ncore/zig-out/lib/*.so app/src/main/jniLibs/arm64-v8a/

    - name: Build with Gradle
      run: ./gradlew assembleRelease

    - name: Upload Release Artifact
      uses: actions/upload-artifact@v5
      with:
         name: release
         path: app/build/outputs/apk/release/*.apk
